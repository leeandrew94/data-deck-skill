---
name: data-deck-skill
description: 从 CSV/Excel 文件自动生成数据驱动的 HTML 演示文稿（单文件），含原生 ECharts 交互图表、KPI 大字报、数据洞察。9 种视觉风格预设，支持导出 PDF。当用户需要做数据周报、月报、销售报表、数据分析汇报、数据 PPT 时使用。
---

# Data Deck · 数据驱动演示文稿

## 这个 Skill 做什么

从一份 CSV 或 Excel 文件生成一份**单文件 HTML 数据演示文稿**。

核心能力：**数据分析 → 图表生成 → 结论提炼 → 统一风格输出**

- 每一页是一张**原生 ECharts 图表**（可 hover 看数值，不是截图）
- 自动从数据中提取关键指标和洞察
- 9 种视觉风格预设，图表风格全局统一
- 键盘 ← → / 滚轮 / 触屏翻页，浏览器直接打开
- 支持导出 PDF（通过 Playwright 截图，保留图表渲染效果）

**仓库地址**：https://github.com/leeandrew94/data-deck-skill

## 何时使用

**合适的场景**：
- 用户给了 CSV/Excel，需要做成汇报/周报/月报
- "帮我做个数据 PPT" / "做个销售报表" / "数据分析汇报"
- 需要精美图表发飞书/邮件/公众号
- 数据需要以演示形式呈现给领导/客户

**不合适的场景**：
- 用户只需要简单看数据（用 pandas/tail 就够了）
- 数据量极大（>50 万行）且需要实时查询（用 BI 工具）
- 用户需要多人协作编辑（这是静态 HTML）

## 工作流

### Step 1 · 需求澄清（动手前必做）

**必须问用户以下 7 个问题**（可合并为一轮对话）：

| # | 问题 | 为什么要问 |
|---|------|-----------|
| 1 | **数据文件在哪？** | 必须先拿到数据才能分析 |
| 2 | **想重点展示哪些指标？** | 决定 KPI 卡片和主图表 |
| 3 | **有特定的分析角度吗？** | 趋势/对比/排名/组成 |
| 4 | **想要哪种视觉风格？** | 9 种风格选一（见 Step 2） |
| 5 | **受众是谁？汇报场景？** | 决定语言风格和深度 |
| 6 | **需要导出 PDF 吗？** | 决定是否运行导出脚本 |
| 7 | **有没有硬约束？** | 必须包含 XX 数据 / 不能出现 YY |

**风格选择参考**（根据用户关键词推荐）：

| 如果用户说... | 推荐风格 |
|---------------|----------|
| "周报"/"月报"/"管理层汇报"/"正式" | 📊 商务简洁 |
| "数据分析"/"产品看板"/"技术汇报" | 🔬 数据科技 |
| "打印"/"邮件"/"简洁"/"清爽" | 📄 极简白 |
| "炫酷"/"深色"/"发布会"/"社媒" | 🌃 暗黑科技 |
| "营销"/"销售"/"复盘"/"暖色" | 🔥 暖色商业 |
| "时尚"/"创意"/"品牌"/"高级感" | 🎨 莫兰迪 |
| "金融"/"年报"/"企业"/"沉稳" | 🌊 深海蓝调 |
| "ESG"/"健康"/"环保"/"增长" | 🌿 清新绿意 |
| "商务汇报"/"年度总结"/"瑞士风" | 💼 商务汇报 |
| 不指定 | 📊 商务简洁（默认） |

**自动模式**：用户说"帮我分析一下"或"自动生成"时，跳过问答：
- 自动选择信息量最大的指标作为 KPI
- 自动选择最能展示数据特征的图表类型
- 默认风格：📊 商务简洁

### Step 1.1 · 数据分析

**读取数据文件**：
- CSV：直接读取，推断分隔符（逗号/制表符/分号）和编码（UTF-8/GBK）
- Excel：用 pandas 或 openpyxl 转为 DataFrame 后分析

**生成数据摘要**（内部参考，不展示给用户）：
- 列数、行数、文件大小
- 每列：名称、推断类型（numeric/categorical/temporal/text）、非空率、唯一值数、min/max/mean（数值列）
- 前 5 行预览

**列类型检测规则**（优先级从高到低）：

| 类型 | 判断条件 |
|------|---------|
| TEMPORAL | 可被 Date 解析，或表头含 "date"/"time"/"日期"/"时间"/"年"/"月"/"日" |
| NUMERIC | >80% 可解析为浮点数，或表头含 "amount"/"count"/"total"/"金额"/"数量"/"价格"/"率" |
| CATEGORICAL | 唯一值 < min(20, 5% 总行数)，或是布尔值 |
| TEXT | 其他（自由文本、ID、名称） |

数值子类型：
- CURRENCY：表头含 "$"/"price"/"amount"/"金额"/"价格"
- PERCENTAGE：表头含 "%"/"rate"/"ratio"/"率"/"比"，且值在 [0,1] 或 [0,100]
- DISCRETE：全部为整数且唯一值 < 30

### Step 2 · 选择视觉风格（已在 Step 1 确认）

从 9 种预设中选一（**不允许自定义颜色**——保护美学比给自由更重要）：

| # | 风格 | 适合 | 默认用于 |
|---|------|------|---------|
| 1 | 📊 商务简洁 | 正式报告、管理层汇报 | 周报/月报/季度汇报 |
| 2 | 🔬 数据科技 | 数据分析、产品看板 | 数据分析/技术汇报 |
| 3 | 📄 极简白 | 打印/邮件、简洁清爽 | 邮件正文/打印 |
| 4 | 🌃 暗黑科技 | 炫酷展示、深色主题 | 社媒/发布会/演示 |
| 5 | 🔥 暖色商业 | 营销报告、销售复盘 | 消费数据/营销汇报 |
| 6 | 🎨 莫兰迪 | 时尚/创意行业 | 品牌报告/生活方式 |
| 7 | 🌊 深海蓝调 | 金融报告、企业年报 | 正式汇报/深色偏好 |
| 8 | 🌿 清新绿意 | ESG/健康/环保数据 | 增长指标/绿色主题 |
| 9 | 💼 商务汇报 | 瑞士国际主义风格 | 正式商务汇报/年度总结 |

> 注意：风格 9（商务汇报）使用 `template-business.html` 模板，采用无衬线字体、直角纯色、极致字号对比。

选择后，读取 `references/themes.md` 获取对应的 CSS 变量值。

### Step 3 · 拷贝模板 + 填充内容

**3.1 拷贝模板**：

```bash
mkdir -p "项目/XXX/deck"
cp "<SKILL_ROOT>/assets/template.html" "项目/XXX/deck/index.html"
```

**3.2 替换主题色**（必做）：

读取 `references/themes.md`，找到选定风格的 `:root` 块，**整体替换**模板中 `:root{...}` 里的主题变量。

**3.3 规划页面结构**：

在动手写 HTML 前，先列一张页面规划表：

```
页码 → 页面类型 → 内容 → 图表类型 → 数据列映射
```

**页面类型**有以下几种（从 `references/layouts.md` 选择）：

| 类型 | 用途 |
|------|------|
| 封面 | 第 1 页：标题 + 副标题 + 日期 |
| KPI 大字报 | 展示 3-5 个关键指标（总值、均值、增长率等） |
| 图表页 | 1-2 张 ECharts 图表 + 标题说明 |
| 洞察页 | 2-4 条数据洞察/结论（文字为主） |
| 表格页 | 原始数据预览（可选，行数 < 100 时） |
| 结束页 | 最后一页：总结/建议/下一步 |

**3.4 填充内容**：

将 `<!-- DECK_SLIDES_HERE -->` 替换为实际的 `<section class="slide">` 代码块。

每个 slide 的基础结构：
```html
<section class="slide">
  <div class="chrome">
    <div>分类标签</div>
    <div>页码 / 总页数</div>
  </div>
  <!-- 主内容：KPI / 图表 / 洞察 -->
  <div class="foot">
    <div>说明文字</div>
    <div>日期</div>
  </div>
</section>
```

**封面页**需加装饰线：
```html
<section class="slide cover">
  <div class="title">标题</div>
  <div class="accent-line"></div>
  <div class="subtitle">副标题</div>
  <div class="meta">日期</div>
</section>
```

**进入动画**（可选）：给需要动画的元素加 `reveal` 类和 `data-anim` 属性：
```html
<div class="reveal" data-anim="up">内容会从下方滑入</div>
<div class="reveal" data-anim="fade">内容会淡入</div>
```

**移动端适配**：模板自动适配手机端（≤768px 切换为垂直滚动模式），无需额外处理。

**3.5 配置 ECharts 图表**：

对每个图表，在 slide 底部的 `<script>` 中创建 ECharts 实例：

```javascript
(function(){
  function init(){
    var chart=echarts.init(document.getElementById('chart-1'),'data-deck');
    chart.setOption({
      // 图表配置 — 参考 references/chart-selection.md 的模板
    });
    window.registerChart(chart);
  }
  if(window.echarts) init();
  else window.addEventListener('echarts-ready',init);
})();
```

**重要**：图表配置必须使用 `references/chart-selection.md` 中的标准模板，不要从零写。

**3.6 嵌入数据**（如果需要表格页或动态计算）：

将数据嵌入 HTML：
```html
<script id="deck-data" type="application/json">
{"columns":[...],"rows":[...]}
</script>
```

**3.7 必改占位符**（grep "[必填]" 确认全部替换完）

### Step 4 · 对照检查清单自检

生成完打开 `references/checklist.md`，逐项对照。P0 级问题必须全部通过。

**必须做的自检**：
1. `grep '[必填]' index.html` — 确认无残留占位符
2. 浏览器打开，逐页检查图表是否正常渲染
3. hover 图表看 tooltip 是否显示正确
4. 检查数字格式（金额有 ¥、百分比有 %、大数有万/亿）
5. 检查 KPI 卡片的涨跌颜色（涨绿跌红）
6. 检查最后一页是否有总结/建议

### Step 5 · 预览

```bash
open "项目/XXX/deck/index.html"
```

### Step 6 · 迭代

根据用户反馈修改。常见迭代：
- 调整图表类型（换 ECharts option）
- 增删页面
- 调整字号/间距（改 inline style）
- 更换主题色（替换 `:root` 变量）

### Step 7 · 导出 PDF（可选）

用户需要 PDF 时，使用导出脚本：

```bash
bash "<SKILL_ROOT>/scripts/export-pdf.sh" "项目/XXX/deck/index.html"
```

脚本会自动：
1. 启动本地 HTTP 服务器（字体和 ECharts 需要 HTTP 环境）
2. 用 Playwright 逐页截图（1920×1080）
3. 合并为单个 PDF
4. 清理临时文件

**可选参数**：
- 指定输出路径：`bash scripts/export-pdf.sh ./index.html ./report.pdf`
- 紧凑模式（更小文件）：`bash scripts/export-pdf.sh ./index.html --compact`

**依赖**：Node.js（Playwright 会自动安装到临时目录，不污染全局）

**注意**：
- 图表交互性在 PDF 中不保留（静态截图）
- 首次运行需下载 Chromium，约 1-2 分钟
- PDF 分辨率默认 1920×1080，`--compact` 为 1280×720

## 图表选择速查

详见 `references/chart-selection.md`。核心规则：

| 数据特征 | 推荐图表 |
|---------|---------|
| 单指标汇总 | KPI 卡片（大数字 + 涨跌） |
| 分类 × 数值 | 柱状图 / 水平条形图 |
| 时间 × 数值 | 折线图 / 面积图 |
| 时间 × 分类 × 数值 | 堆叠柱状图 / 多折线 |
| 分类 × 数值（占比） | 饼图（≤6 类）/ 环形图 |
| 排名 | 水平条形图（Top N） |
| 分布 | 直方图 / 箱线图 |
| 多维对比 | 雷达图（≤8 维） |
| 相关性 | 散点图 / 气泡图 |

**反模式**（绝对不能做）：
- 饼图 > 8 个分类
- 折线图用于非连续 X 轴
- 数值轴不从 0 开始（除非有充分理由）
- 一个页面堆超过 3 张图表

## 关键设计原则

1. **每页一个重点** — 一张图表说清楚一件事，不要堆砌
2. **图表不是装饰** — 每张图表必须有明确的信息传达目的
3. **数字会说话** — KPI 大字报比图表更直观，优先放关键数字
4. **结论比数据重要** — 洞察页的价值 > 图表页
5. **风格全局统一** — 一份 deck 只用一套主题色，图表配色一致
6. **hover 即详情** — 图表的 tooltip 要包含完整信息，不要让用户猜
